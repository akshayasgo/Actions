name: manual trigger test
on:
  workflow_dispatch
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Build manual test
        run: echo " this is test we are doing by running it manually"
